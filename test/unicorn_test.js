'use strict';

var grunt = require('grunt');

// TODO: write relevant test for something unicorn-y
exports.unicorn = {
  setUp: function(done) {
    // setup here if necessary
    done();
  },
  default_options: function(test) {

    test.done();
  },
  custom_options: function(test) {

    test.done();
  },
};
